name: Build & Release

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up JDK 25
        uses: actions/setup-java@v4
        with:
          java-version: '25'
          distribution: 'oracle'

      - name: Set up Gradle 9.4
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '9.4.1'

      # Added working-directory here
      - name: Make gradlew executable
        run: chmod +x gradlew
        working-directory: InputBooster

      - name: Build with Gradle wrapper
        run: ./gradlew build
        working-directory: InputBooster

      - name: Read mod version
        id: version
        working-directory: InputBooster
        run: |
          VERSION=$(grep "^version" build.gradle | head -1 | sed "s/version = '//;s/'//")
          echo "MOD_VERSION=$VERSION" >> $GITHUB_OUTPUT
          echo "TAG=v$VERSION" >> $GITHUB_OUTPUT
          echo "Detected version: $VERSION"

      - name: Check if tag already exists
        id: tag_check
        run: |
          if git ls-remote --tags origin "refs/tags/v${{ steps.version.outputs.MOD_VERSION }}" | grep -q .; then
            echo "TAG_EXISTS=true" >> $GITHUB_OUTPUT
          else
            echo "TAG_EXISTS=false" >> $GITHUB_OUTPUT
          fi

      - name: Extract changelog for this version
        id: changelog
        if: steps.tag_check.outputs.TAG_EXISTS == 'false' && github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: |
          VERSION="${{ steps.version.outputs.MOD_VERSION }}"
          # Adjusted path to CHANGELOG.md if it is inside the subfolder
          NOTES=$(awk "/^## \[$VERSION\]/{found=1; next} found && /^## \[/{exit} found{print}" InputBooster/CHANGELOG.md)
          echo "$NOTES" > /tmp/release_notes.md
          echo "Release notes extracted for v$VERSION"

      - name: Create Git tag
        if: steps.tag_check.outputs.TAG_EXISTS == 'false' && github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git tag -a "${{ steps.version.outputs.TAG }}" -m "Release ${{ steps.version.outputs.TAG }}"
          git push origin "${{ steps.version.outputs.TAG }}"

      - name: Find built JAR
        id: jar
        run: |
          # Adjusted path to look inside the InputBooster/build folder
          JAR=$(find InputBooster/build/libs -name "inputbooster-*.jar" ! -name "*-sources.jar" | head -1)
          echo "JAR_PATH=$JAR" >> $GITHUB_OUTPUT
          echo "Found JAR: $JAR"

      - name: Create GitHub Release
        if: steps.tag_check.outputs.TAG_EXISTS == 'false' && github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.version.outputs.TAG }}
          name: "InputBooster ${{ steps.version.outputs.TAG }}"
          body_path: /tmp/release_notes.md
          files: ${{ steps.jar.outputs.JAR_PATH }}
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
